import { LazLoadDirective } from './laz-load.directive';

describe('LazLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new LazLoadDirective();
    expect(directive).toBeTruthy();
  });
});
